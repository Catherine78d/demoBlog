{% extends "base.html.twig" %}

{% block body %}

    <h1 class="display-4 text-center my-4">Création d'un article</h1>

    {# <form method="post" action="" class="col-md-8 mx-auto">
        <div class="form-group">
            <label for="title">Title</label>
            <input type="text" name="title" class=" form-control" id="title">
        </div>
        <div class="form-group">
            <label for="content">Content</label>
            <textarea class="form-control" id="content" name="content" rows="7"></textarea>
        </div>
        <div class="form-group">
            <label for="image">Image</label>
            <input type="text" name="image" class=" form-control" id="image">
        </div>
        <button type="submit" class="btn btn-primary">Enregistrer</button>
    </form> #}

    {# formulaire brut #}
    {# {{ form(formArticle) }} #}


    {{ form_start(formArticle, { 'attr' : {'class' : 'col-md-5 mx-auto'} }) }}

        {{ form_row(formArticle.title, { 'attr' : {'placeholder' : "Titre de l'article", 'title': "entre 2 et 50 caractères"}, 'label': "Titre de l'article" }) }}

        {{ form_row(formArticle.category, {'label': "Catégorie" }) }}

        {{ form_row(formArticle.content, { 'attr' : {'placeholder' : "Contenu de l'article", "rows": 15}, 'label': "Contenu de l'article" }) }}

        {{ form_row(formArticle.image, { 'attr' : {'placeholder' : "URL de l'image"}, 'label': "URL de l'image" }) }}


        <button type="submit" class="btn btn-primary">
        {# editMode est une variable qui est envoyée par le controller, de la fonction form() #}
        {#  Si editMode renvoie TRUE, cele veut dire que l'article possède un ID, donc c'est une modification #}
        {% if editMode %}
            Enregistrer les modifications

        {%  else %}
        {# Sinon cela veut dire que l'article ne possède pas d'ID, c'est donc une insertion #}
            Enregistrer l'article
        
        {% endif %}
        </button>

    {{ form_end(formArticle) }}
    
{% endblock %}

